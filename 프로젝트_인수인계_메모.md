# 토지 실거래가 지번 복원 플랫폼 — 프로젝트 인수인계 메모

> **Claude Code에게**: 이 파일을 먼저 읽어주세요. 사용자(J)는 개발 경험이 거의
> 없는 분입니다. 터미널·코딩 용어를 최소화하고, 한 번에 한 단계씩, 무엇을 어디에
> 입력/클릭하는지 아주 구체적으로 안내해 주세요. 빨간 에러가 나면 그 화면을 보고
> 원인을 짚어주세요. 절대 여러 단계를 한꺼번에 시키지 마세요.

---

## 1. 우리가 만들려는 것 (한 줄 요약)

"용인 318지방도 인근 임야 실거래가"처럼 **자연어로 물어보면**, 국토부 실거래가를
정리해 보여주는 플랫폼. 핵심은 국토부가 별표(예: `산 5**`)로 가려놓은 토지 지번을
**정확한 지번(예: 산 542-3)으로 복원**하는 엔진. (밸류맵·디스코가 쓰는 방식과 동일)

## 2. 지번 복원의 원리 (핵심 자산)

국토부 토지 실거래가 API는 본번을 별표 처리(`5**`)하고 부번은 비공개.
하지만 다음 두 데이터를 **면적·지목·본번대로 교차 대조**하면 지번이 복원됨:

- **거래 데이터** (국토부): 법정동 + 지목 + 거래면적 + 본번(별표) + 거래금액 + 계약일
- **지적 데이터** (V-World 연속지적도 `LP_PA_CBND_BUBUN`): 그 법정동 전체 필지의
  정확한 지번 + 면적 + 지목 + 좌표 (GeoJSON, `pnu:like:{법정동코드10자리}` 필터)

매칭 결과는 3단계 신뢰도로 분류:
- **확정(high)**: 면적 일치 필지가 1개 → 지번·좌표 확정
- **후보다수(mid)**: 2~3개 → 사용자 확인 필요 (자동확정 보류)
- **실패(low)**: 0개 또는 다수 → 법정동 단위로만 표시

> PNU(필지고유번호) 구조: 법정동코드10 + 산구분1 + 본번4 + 부번4 = 19자리.
> 본번은 PNU의 끝에서 8~5번째 자리(`pnu[-8:-4]`).

## 3. 지금까지 완료한 것

- [x] 데이터 가용성·API 스펙 조사 완료 (국토부 토지매매 실거래가, V-World 연속지적도)
- [x] **매칭 엔진 프로토타입 완성·검증** → 같은 폴더의 `jibun_matcher.py`
      - 모의 데이터로 4건 중 3건 자동복원(75%), 1건은 면적중복으로 안전하게 "후보다수" 분류
      - 핵심 로직(본번대 추출 / 면적 교차매칭 / 신뢰도 분류) 작동 확인됨
- [x] V-World 인증키 발급 완료
- [x] Claude Code 설치 완료 (Windows native)

## 4. 다음에 할 일 (이 순서대로)

1. **국토부 실거래가 API 키 발급 마무리**
   - data.go.kr 로그인까지 완료된 상태. "토지 매매 실거래가" 검색 →
     "국토교통부_토지 매매 실거래가 자료"의 **오픈API** 항목 → "활용신청" 클릭.
   - ⚠️ 키(인증키)는 비밀번호이므로 채팅에 붙여넣지 말 것. 코드에 직접 넣도록 안내만.

2. **파이썬(Python) 설치 확인**
   - `jibun_matcher.py`를 실제로 돌리려면 Python 필요. 설치 여부부터 확인.
   - (Windows: `python --version` 입력해보고 없으면 python.org 또는 winget으로 설치)

3. **엔진을 내 컴퓨터에서 직접 실행** → `python jibun_matcher.py`로 데모 결과 재현

4. **실데이터 연결**: `jibun_matcher.py` 안의 `fetch_molit_trades()`와
   `fetch_vworld_parcels()` 함수의 스텁(주석 처리된 부분)을 실제 `requests` 호출로 교체.
   - 두 API 모두 CORS 때문에 브라우저 직접호출 불가 → Python 백엔드에서 호출.
   - V-World 면적과 신고면적의 반올림 차이 → 허용오차(area_tol) 이미 반영됨.

5. **후보다수 줄이기 (2차 매칭)**: 거래금액·주변시세, V-World 토지이동이력
   (분할·합병 필지의 과거 면적 역추적)으로 후보 좁히는 로직 추가.

6. **자연어 질의 인터페이스 + 화면(UI)** 붙이기 — 마지막 단계.

## 5. 알아둘 현실적 한계 (정직하게)

- 면적이 흔한 값(예: 990㎡대)이면 후보가 안 좁혀져 복원 실패/오매칭 가능.
  밸류맵도 100% 정확하지 않음 — 틀린 지번을 우기는 것보다 "보류"가 안전.
- 분할·합병된 필지는 거래 당시 면적 ≠ 현재 지적면적이라 매칭이 깨질 수 있음.
- "도로에서 정확히 1km" 정밀 반경은 좌표 복원이 된 필지에 한해 가능.

## 6. 사용자(J) 배경 참고

- 권리분석·부동산 실사에 강점. RF인증/규제 실무 경험으로 데이터 정합성 감각 있음.
- Windows PC, 터미널 경험 매우 적음, "직접 따라 하며 배우는" 방식 선호.
