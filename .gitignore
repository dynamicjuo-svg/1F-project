# Python
__pycache__/
*.pyc
*.pyo

# 환경변수 / 키 (절대 push 금지)
.env
.env.local

# SQLite 데이터베이스 (크기 큼, Railway에서 빌드 시 재생성)
trades.db
*.db
*.db-journal
*.db-wal
*.db-shm
*.db.bak*

# V-World 필지 캐시 (build 시 자동 생성됨, 크기 큼)
cache_*.json
wonsam_cache.json
# region_prefix_cache.json은 작은 정적 매핑이라 GitHub에 올림

# DEM 타일 (build_dem.py가 AWS public S3에서 자동 다운로드)
*.hgt
*.hgt.gz

# Streamlit 정적/시크릿
static/naver_map.html
static/*.html
.streamlit/secrets.toml

# IDE
.idea/
.vscode/

# 임시 디버깅 스크립트 (앞머리 _ 표기)
_*.py
